# Example only — DO NOT apply with real values. Prefer:
#   kubectl create secret generic aihub-staging-core -n aihub-staging --from-env-file=...
#
# Required keys for deployment-api.yaml:
#   database_url
#   redis_url
# Optional:
#   vault_token
apiVersion: v1
kind: Secret
metadata:
  name: aihub-staging-core
  namespace: aihub-staging
type: Opaque
stringData:
  database_url: postgresql://user:password@postgres-host:5432/aihub_staging
  redis_url: redis://redis-host:6379
  vault_token: ""
