apiVersion: v1
kind: Service
metadata:
  name: aihub-api
  namespace: aihub-staging
  labels:
    app.kubernetes.io/name: aihub-api
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: aihub-api
  ports:
    - name: http
      port: 3001
      targetPort: http
