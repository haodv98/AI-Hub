apiVersion: v1
kind: ConfigMap
metadata:
  name: aihub-api-prod-env
  namespace: aihub-prod
  labels:
    app.kubernetes.io/name: aihub-api
    environment: production
data:
  NODE_ENV: production
  PORT: "3001"
  LOG_LEVEL: info
