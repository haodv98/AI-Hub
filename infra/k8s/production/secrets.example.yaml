# Example only — DO NOT commit real values. Create secrets with kubectl / ExternalSecrets.
apiVersion: v1
kind: Secret
metadata:
  name: aihub-prod-core
  namespace: aihub-prod
type: Opaque
stringData:
  database_url: postgresql://user:password@postgres-host:5432/aihub_prod
  redis_url: redis://redis-host:6379
  vault_token: ""
